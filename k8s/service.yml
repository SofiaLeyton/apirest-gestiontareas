apiVersion: v1
kind: Service

metadata:
  name: todo-api-service
  namespace: todo-app

spec:
  selector:
    app: todo-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000

  type: NodePort